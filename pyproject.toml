[project]
name = "automate"
version = "0.1.0"
description = "Assistant Numérique de Covoiturage Universel Libre et Évolutif"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "moshi>=0.2.11",
]
